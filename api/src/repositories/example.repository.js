// Example repository

// Repositories handle direct interactions with the database or external data sources
export const exampleRepository = () => {
  // Example: This is where you would add database queries
  return { key: 'value from database' };
};
